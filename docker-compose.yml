services:
  app:
    image: ${YANAI_IMAGE:-huaiyuechusan/yanai:latest}
    container_name: yanai
    restart: unless-stopped
    ports:
      - "${YANAI_PORT:-3000}:80"
    volumes:
      # Keep secrets and mutable runtime state outside of the public image.
      - ./data:/app/data
      - ./config.json:/app/config.json
      # 挂载本地补丁目录，让预构建镜像用上改过的 utils/services（sentinel solver 接线）
      - ./utils:/app/utils
      - ./services:/app/services
    environment:
      STORAGE_BACKEND: ${STORAGE_BACKEND:-json}
      # DATABASE_URL: sqlite:////app/data/accounts.db
      # DATABASE_URL: postgresql://user:password@host:5432/dbname
      # GIT_REPO_URL: https://github.com/user/private-data-repo.git
      # GIT_TOKEN: your_git_token_here
      # GIT_BRANCH: main
      # GIT_FILE_PATH: accounts.json
      # CHATGPT2API_AUTH_KEY: your_secret_key_here
      # CHATGPT2API_BASE_URL: https://your-domain.com
